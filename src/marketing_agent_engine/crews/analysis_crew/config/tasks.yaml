routing_task:
  description: >
    Analyze the following Asana ticket and determine its cluster and business unit
    using the route_ticket domain tool.

    Ticket data:
    {ticket_yaml}

    IMPORTANT — Before calling the route_ticket tool:
    1. Read the entire ticket (title, notes, html_notes) and ALL custom fields, tags, and project names.
       Write a short 2-sentence summary explaining what this ticket is trying to achieve.
    2. Semantically identify the likely domain/cluster and business unit. Do not just look for hardcoded keywords;
       understand the context. Crawl all values of all ticket parameters to check if a specific Business Unit
       (e.g. "M-SOFT", "Pinncalc", "blue:solution", etc.) or Cluster is mentioned anywhere in the text, tags, or custom fields.
       For example:
       - Replacing a boiler or installing a heat pump belongs to "SHK+E" (Sanitär, Heizung, Klima, Energie).
       - Timber construction or roof repairs belongs to "Dach_und_Holz".
       - Foundation work or masonry belongs to "Baugewerbe".
    3. Use your semantic understanding as context when calling the tool.
    4. The tool will use both keyword matching and project GID to determine routing.

    Steps:
    1. Write your Chain-of-Thought analysis of the ticket's domain.
    2. Call the route_ticket tool with no arguments to get the structural routing result.
    3. Evaluate if the tool's output matches your semantic understanding. If the tool says "unbekannt"
       but you are certain it is "SHK+E", you may override the cluster in your final JSON output.
    4. Return the full RoutingResult as structured JSON.

    Your output must include:
    - cluster (string)
    - business_unit (object or null)
    - cluster_coordinator_id (string or null)
    - confidence (float)
    - resolution_path (list of strings)
  expected_output: >
    First, your 2-sentence semantic analysis. Then, a JSON object with keys: cluster, business_unit,
    cluster_coordinator_id, confidence, resolution_path.
  agent: routing_analyst

assignment_task:
  description: >
    Analyze the assignee and recommend replacements for the following Asana ticket
    using the analyse_assignment domain tool.

    Ticket data:
    {ticket_yaml}

    Known routing result (use cluster_slug for scoped recommendations):
    {routing_result_json}

    Steps:
    1. Write a 2-sentence analysis explaining what specific skills are actually required
       to complete the work described in the ticket text.
    2. Call the analyse_assignment tool with no arguments to get the assignment analysis.
    3. Compare the tool's structural recommendations with your semantic skill analysis.
    4. Return the full AssignmentAnalysis as structured JSON.
    5. Include the plausibility verdict for the current assignee.
    6. Include up to 3 ranked recommendations with employee_id, display_name,
       confidence, matched_skills, reason, and ai_readiness.
    7. Set needs_reassignment and reassignment_reason accurately.
    8. Always include the new AI-readiness fields in the output.

    Your output must include:
    - current_assignee_verdict (object or null)
    - recommendations (list, max 3)
    - needs_reassignment (bool)
    - reassignment_reason (string)
    - task_category (string — e.g. "HubSpot/Marketing-Automation")
    - ai_readiness_required (string — minimum AI-readiness for the task)
    - assignee_readiness (string or null — AI-readiness of the current assignee)
    - recommended_assignee_ai_level (string or null — AI-readiness of top recommendation)
    - assigned_by_skill_matrix (bool — true when no assignee was set and skill matrix was used)
  expected_output: >
    First, your 2-sentence skill analysis. Then, a JSON object with keys: current_assignee_verdict, recommendations,
    needs_reassignment, reassignment_reason, task_category, ai_readiness_required,
    assignee_readiness, recommended_assignee_ai_level, assigned_by_skill_matrix.
  agent: assignment_analyst
  context:
    - routing_task

completeness_task:
  description: >
    Evaluate the completeness of the following Asana ticket using a Two-Pass approach.

    Ticket data:
    {ticket_yaml}

    Steps:
    1. Pass 1 (Semantic): Read the entire ticket title, notes/html_notes, and any included
       comments/stories. Write a short analysis of whether the ticket contains enough
       context to be actionable. Is there a clear goal? Is there a deadline mentioned in the text
       even if the formal due_date field is empty?
    2. Pass 2 (Structural): Call the check_completeness tool with no arguments to evaluate
       the ticket against the rigid python rules.
    3. Override (Crucial): If the tool flags a field as missing (e.g. "due_date_set": false, or
       "description_present": false because the text is too short), but your semantic analysis
       proves the information IS present in the text or comments, you MUST override that flag.
       Mark it as passed, increase the score, and remove it from the "missing" list.
    4. List every criterion that did NOT pass under "missing".
    5. List all warning messages under "warnings".
    6. Categorize each missing item by responsible party:
       - "requester" — the person who created the ticket must supply this
       - "coordinator" — the cluster coordinator must fill this in
       - "system" — should be auto-populated by Asana (e.g. workspace, permalink)

    Your output must include:
    - score (float, 0–1) — recalculated if you overrode the tool
    - passed (bool)
    - missing (list of strings)
    - warnings (list of strings)
    - missing_by_owner (object: {requester: [], coordinator: [], system: []})
  expected_output: >
    First, your Two-Pass semantic analysis. Then, a JSON object with keys: score, passed,
    missing, warnings, missing_by_owner.
  agent: completeness_analyst
  context:
    - routing_task

synthesis_task:
  description: >
    You have received outputs from three specialist agents. Synthesize them into
    one final structured decision object for this Asana ticket.

    Ticket data:
    {ticket_yaml}

    Routing result: (from routing_task output)
    Assignment analysis: (from assignment_task output)
    Completeness evaluation: (from completeness_task output)

    Build a final JSON decision with this structure:
    {{
      "ticket_id": "<task GID or title>",
      "dry_run": {dry_run},
      "execution_mode": "dry_run",
      "model_used": "{model}",
      "timestamp": "<iso8601 UTC timestamp>",
      "routing": {{
        "cluster": "...",
        "business_unit_slug": "...",
        "routing_confidence": 0.0,
        "coordinator_id": "..."
      }},
      "assignment": {{
        "verdict": "plausible|questionable|implausible|unknown",
        "assignment_confidence": 0.0,
        "needs_reassignment": false,
        "reassignment_reason": "...",
        "task_category": "...",
        "ai_readiness_required": "AI-Bereit|AI-Erfahren|AI-Pioneer|AI-Novice",
        "assignee_readiness": "...",
        "recommended_assignee_ai_level": "...",
        "assigned_by_skill_matrix": false,
        "top_recommendation": {{
          "employee_id": "...",
          "display_name": "...",
          "confidence": 0.0,
          "matched_skills": []
        }}
      }},
      "completeness": {{
        "completeness_score": 0.0,
        "passed": false,
        "missing_count": 0,
        "critical_missing": []
      }},
      "actions": [
        {{
          "priority": 1,
          "action": "...",
          "owner": "...",
          "reason": "..."
        }}
      ],
      "overall_status": "ready|needs_attention|blocked",
      "decision_trace": {{
        "routing": "<one sentence: how cluster/BU was resolved, what matched, confidence level>",
        "assignment": "<one sentence: why verdict was set, which skills matched or were missing>",
        "completeness": "<one sentence: score, how many criteria passed, which ones failed>"
      }}
    }}

    Rules for overall_status:
    - "ready": routing_confidence >0.6 AND assignment plausible AND completeness passed
    - "needs_attention": any one of routing/assignment/completeness has a warning
    - "blocked": cluster=unbekannt OR completeness_score < 0.5 OR assignment=implausible

    Rules for execution_mode: always set to "dry_run" when {dry_run}=true, else "live".
    Rules for timestamp: current UTC time in ISO 8601 format (e.g. 2025-04-15T10:30:00Z).
    Rules for routing_confidence: copy from routing agent output field "confidence".
    Rules for assignment_confidence: use top recommendation "confidence" if assignee unknown,
      else use 1.0 for plausible, 0.5 for questionable, 0.1 for implausible, 0.0 for unknown.

    Rules for decision_trace:
    - routing: summarize how the cluster was resolved (matched keywords, GID, or fallback)
      and state the confidence. Example: "Routed to SHK+E via keyword match on 'wärmepumpe'
      (confidence 0.69); coordinator emp_002 assigned."
    - assignment: explain the verdict with evidence. Example: "Assignee Felix Hoffmann
      is implausible — task requires seo/analytics but employee covers design/social_media only."
    - completeness: state score and list failed criteria. Example: "Score 0.73 (8/11);
      failed: tags_present, custom_fields_filled, followers_present."
    - Each value must be a single human-readable sentence. No nested objects. No lists.

    Rules for actions list:
    - Prioritize by severity (blocked > needs_attention)
    - Each action must have: priority (int), action (string), owner (string), reason (string)
    - Do NOT invent actions not supported by the upstream agent outputs
    - Maximum 5 actions

    IMPORTANT: Output ONLY the JSON object. No prose before or after.
  expected_output: >
    A single valid JSON object matching the structure above exactly.
    No markdown fences. No explanatory text. Only the JSON.
  agent: decision_synthesizer
  context:
    - routing_task
    - assignment_task
    - completeness_task
